// BLOCO 1: WRITE THE INITIALS OR FINALS YOU HEAR
const initialsFinalsBlock = {
  // Pode adicionar funcionalidades específicas para este bloco aqui
  init: function() {
    console.log('Inicializando seção de iniciais/finais');
  }
};

// Inicializar quando o DOM estiver pronto
if (document.getElementById('initials-finals-block')) {
  initialsFinalsBlock.init();
}