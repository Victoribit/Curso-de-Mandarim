// BLOCO 3: WRITE THE SYLLABLES YOU HEAR
const syllablesBlock = {
    // Pode adicionar funcionalidades específicas para este bloco aqui
    init: function() {
      console.log('Inicializando seção de sílabas');
    }
  };
  
  // Inicializar quando o DOM estiver pronto
  if (document.getElementById('syllables-block')) {
    syllablesBlock.init();
  }